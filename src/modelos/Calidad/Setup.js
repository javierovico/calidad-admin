import ClaseModelV3 from "../ClaseModelV3";

export default class Setup extends ClaseModelV3{
    static URL_DESCARGA = `/pbx/setup`;

    constructor(atributos) {
        super({atributos});
    }

}